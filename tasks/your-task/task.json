{
  "id": "react-cart-discount",
  "description": "There is a bug in `src/utils/calculateTotal.js`. The function subtracts the discount twice on the return line. Fix it so all tests pass.",
  "source_path": "./repo",
  "evaluation": {
    "commands": ["npm install", "npm test"]
  }
}
